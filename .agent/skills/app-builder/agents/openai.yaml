interface:
  display_name: "App Builder"
  short_description: "Turn product requests into buildable app execution paths"
  brand_color: "#2563EB"
  default_prompt: "Use $app-builder to classify this app request, choose the right stack, and route the supporting skills."

policy:
  allow_implicit_invocation: true
