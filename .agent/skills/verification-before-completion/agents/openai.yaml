interface:
  display_name: "Verification Before Completion"
  short_description: "Require fresh evidence before saying work is done"
  brand_color: "#B91C1C"
  default_prompt: "Use $verification-before-completion before claiming this task is fixed, passing, or complete."

policy:
  allow_implicit_invocation: true
