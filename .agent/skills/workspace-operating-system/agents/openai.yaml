interface:
  display_name: "Workspace Operating System"
  short_description: "Route tasks, choose skills, and enforce delivery quality"
  brand_color: "#0F766E"
  default_prompt: "Use $workspace-operating-system to choose the right skill stack, sequence the work, and apply the repository quality bar."

policy:
  allow_implicit_invocation: true
